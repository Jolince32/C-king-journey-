#include <stdio.h>

int main() {
    int marks[] = {80, 90, 70}; // 3 dabba iruku

    // i = 0 la irundhu start = First door la irundhu
    for(int i = 0; i < 3; i++) {
        printf("Door %d = %d\n", i, marks[i]);
    }
    return 0;
}